l = [[1], 2, 3]
print(l is l.copy())
print(l[0] is l.copy()[0])
"""
SUM(buy_book.amount)

SELECT name_genre, buy_book.amount AS Количество
FROM buy_book
LEFT JOIN book ON buy_book_id = book.book_id
LEFT JOIN genre ON genre.genre_id = book.genre_id

;

"""